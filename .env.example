# Local: aponta pro backend rodando na sua máquina.
# Produção (CapRover): passe como build arg VITE_API_URL, ex.
# https://api.gustagg.app — é lido em build time (vira parte do bundle
# estático), não em runtime.
VITE_API_URL=http://localhost:3000
